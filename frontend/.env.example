# API суурь (заавал биш). Хоосон бол Vercel дээр `/api` rewrite ашиглана.
# Local docker-compose:
# REACT_APP_API_URL=http://localhost:5500/api
#
# Optional Google Maps (map features work in demo mode without this key)
# REACT_APP_GOOGLE_MAPS_API_KEY=
